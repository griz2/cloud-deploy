info:
  name: Test 1
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/applications/1/deployments
  body:
    type: json
    data: |-
      {
        "branch": "main",
        "commitSha": "test-docker-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
