info:
  name: Test 1
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/applications/1/deployments
  body:
    type: json
    data: |-
      {
        "commitSha": "abc123",
        "branch": "main"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
