info:
  name: Test 1
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/applications/5/deployments
  body:
    type: json
    data: |-
      {
        "branch": "main",
        "commitSha": "git-clone-test-006"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
