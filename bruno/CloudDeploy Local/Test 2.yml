info:
  name: Test 2
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/applications/6/deployments
  body:
    type: json
    data: |-
      {
        "branch": "main",
        "commitSha": "docker-build-test-008"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
