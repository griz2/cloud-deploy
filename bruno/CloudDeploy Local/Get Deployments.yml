info:
  name: Get Deployments
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/applications/1/deployments
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
